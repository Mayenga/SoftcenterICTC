stakeholders = {
    user_id, 
    email, 
    phone, 
    org_name, 
    no_staffs, 
    founders_names, 
    status, //registered or non-registered, to select
    est_year, 
    org_based, 
    pref_startup_stage, 
    fund_sources, 
    focus_sector, //to select
    program_duration, 
    business_model, //to select
    financial_stage, //to select
}

focus_sectors = {
    id,
    sector_name,
}

startups = {
    //org names, status, product_sector, est_year
    user_id, 
    email, 
    phone, 
    ownership, //individual or organization
    gender, 
    product_name, 
    product_description, 
    no_staffs, 
    founders_names, 
    status, //registered or non-registered
    est_year, 
    org_based, 
    product_sector, 
    product_stage, //to select
    business_model, 
    financial_stage
}

announcements = {
    id, 
    type, 
    content
}